version: '3.8'

services:
  tengri-game:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tengri-game
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    restart: unless-stopped
